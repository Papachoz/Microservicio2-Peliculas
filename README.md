-- Clonar proyecto
-- Correr:
   docker compose up -- esto crea y corre el contenedor de MySQL y lo conecta al programa
-- para probar el programa ejecutar
  npm run dev
-- para porobar en postaman sería 
  http://localhost:3000/api/ 
-- para ver los endopints y el formato para los posts, puedes ver los scheamas del codigo o poner en el buscador 
   teniendo del back corriendo  http://localhost:3000/docs

  
